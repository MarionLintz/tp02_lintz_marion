import { PasswordControl } from './password-control.directive';

describe('InvalidDataDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidDataDirective();
    expect(directive).toBeTruthy();
  });
});
